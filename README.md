# NeuralAmpModeler Workspace

This repository contains the JavaScript/Python implementation. C# work should live in the dedicated [NAM_CS](https://github.com/sshukes/NAM_CS) repository as requested. The previous `csharp` stub was removed to avoid confusion.
